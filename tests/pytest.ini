[pytest]
markers =
    slow: mark a test as slow
    ultra_slow: mark a test as very slow which is normaly not tested
    gitlabonly: should only be tested on gitlab, because github has not enough resources